[package]
name = "rhizome-resin-procgen"
version = "0.1.0"
edition = "2024"
description = "Procedural generation algorithms (WFC, mazes, roads, rivers)"
license = "MIT OR Apache-2.0"
repository = "https://github.com/rhizome-lab/resin"

[dependencies]
glam.workspace = true
