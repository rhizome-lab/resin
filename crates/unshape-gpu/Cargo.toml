[package]
name = "unshape-gpu"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Core types and traits for unshape fields"

[features]
serde = ["dep:serde"]
dynop = ["dep:unshape-op", "dep:serde_json", "serde"]
image-expr = ["dep:unshape-image", "dep:rhizome-dew-linalg"]

[dependencies]
glam = { workspace = true }
wgpu = "23"
bytemuck = { version = "1.14", features = ["derive"] }
pollster = "0.4"
thiserror = { workspace = true }
unshape-core = { workspace = true }
unshape-backend = { workspace = true }
unshape-op = { path = "../unshape-op", optional = true }
unshape-image = { path = "../unshape-image", features = ["wgsl"], optional = true }
rhizome-dew-linalg = { git = "https://github.com/rhizome-lab/dew", features = ["4d", "wgsl"], optional = true }
serde = { workspace = true, optional = true }
serde_json = { version = "1", optional = true }

[dev-dependencies]
image = "0.25"
